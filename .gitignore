# Build / test artifacts
/coverage.out
*.test
*.out
*.prof
/dist/

# Go fuzz corpus cache (keep committed seed corpora under testdata/)
/internal/**/testdata/fuzz/

# Editor / OS
/.idea/
.DS_Store

# Local-only Claude settings
/.claude/settings.local.json
*.db*

# Локальные секреты compose-стека (шаблон — .env.example, он коммитится)
/.env
